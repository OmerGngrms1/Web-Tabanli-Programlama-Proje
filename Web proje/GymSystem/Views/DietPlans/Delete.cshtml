@model GymSystem.Models.DietPlan

@{
    ViewData["Title"] = "Plan Sil";
}

<div class="row justify-content-center">
    <div class="col-md-6">
        <div class="card shadow border-danger">
            <div class="card-header bg-danger text-white">
                <h4 class="mb-0">Plan Silinecek</h4>
            </div>
            <div class="card-body">
                <p class="lead">Bu diyet planını silmek istediğinize emin misiniz?</p>
                <dl class="row">
                    <dt class="col-sm-4">@Html.DisplayNameFor(model => model.Name)</dt>
                    <dd class="col-sm-8">@Html.DisplayFor(model => model.Name)</dd>
                    
                    <dt class="col-sm-4">@Html.DisplayNameFor(model => model.Details)</dt>
                    <dd class="col-sm-8">@Html.DisplayFor(model => model.Details)</dd>
                </dl>
                
                <form asp-action="Delete">
                    <input type="hidden" asp-for="Id" />
                    <div class="d-flex justify-content-between mt-4">
                        <a asp-action="Index" class="btn btn-secondary">İptal</a>
                        <button type="submit" class="btn btn-danger">Evet, Sil</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
